# Replace both password placeholders before starting the stack.
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=replace-with-a-unique-password
MYSQL_ROOT_PASSWORD=replace-with-a-different-root-password
HOST_PORT=8080
ADMINER_PORT=8081
