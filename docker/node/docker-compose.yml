# Copy this file to the application repository root as docker-compose.yml.
services:
  app:
    build:
      context: .
      dockerfile: docker/node/Dockerfile
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-3000}:3000"
    restart: unless-stopped
